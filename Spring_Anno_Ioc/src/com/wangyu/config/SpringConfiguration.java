package com.wangyu.config;

import org.springframework.context.annotation.Configuration;

/**
 * Spring的配置类，作用相当于bean.xml文件
 */
@Configuration//它就相当于表明当前类的spring的配置类
public class SpringConfiguration {
    
}
